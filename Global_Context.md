# Kambala — Global Context

> Веде Claude (L2). Звірено з `docs/technical_specification.md` і мануалом FlyByIP IP-Video v1.3 (2026-09-11). Де ТЗ і фактичні рішення Gans розходяться — пріоритет у рішень Gans, розходження позначено «⚠ ТЗ:». Пункти з «🔶 РІШЕННЯ» чекають затвердження Gans до постановки продуктових задач.

## Екосистема
«Камбала» — розподілена система базування, передстартової підготовки, зарядки та почергового запуску БпЛА (drone-in-a-box): виносні вузли (радіо-відеоблок + 1..N пускових боксів), робочі місця операторів і центральний сервер, що зв'язує їх через VPN поверх Starlink/LTE (CGNAT → лише вихідні тунелі від вузлів і робочих місць).

1.  **Виносний вузол (Node)** — *залізо, пізніше підпроєкт `node` (KNODE)*
    *   **Streamer Unit** (на щоглі): FlyByIP Streamer (CM4) — захоплення відео з аналогового VRX (AV-порт) або Walksnail VRX (HDMI), апаратне кодування H.264/MJPEG, **один активний потік** на вибір; вбудований модуль керування **FlyByIP-B**: S-Port → ExpressLRS TX (CRSF 400 000 бод, до 250 Гц), додатковий UART (UART2, режим Bridge `Dual` / `TCP Server`) → апаратний міст UART↔CAN → лінія боксів.
    *   **Лінія виносу** (до 50 м): CAN (термінатори 120 Ом, multi-drop, 10+ боксів) + транзит PoE 48–54 В (промисловий інжектор 120–240 Вт).
    *   **Launch Box** (1..N): контролер RPi Pico 2 (RP2350) на CAN з фіксованим MAVLink System/Component ID; заряд CC/CV + балансир; BEC 5/3.3 В і силовий BEC для серв; сервоприводи замків/стулок; клімат (SHT3x/BME280, DS18B20, нагрів/вентиляція); кінцевики стулок, датчик наявності дрона; сигнал активації бортового MOSFET-ключа дрона.
    *   **Мережа вузла:** маршрутизатор MikroTik RB750UPr2 — WireGuard-клієнт до сервера, Streamer/FBI-B за ним (без RPi на вузлі). ⚠ Мануал FlyByIP рекомендує ZeroTier — не використовуємо, тунель робить MikroTik.
    *   **Sequential Launch Protocol:** Standby (бокси закриті, борти знеструмлені, заряд/підтримка) → підготовка обраного боксу (завершити заряд → активувати борт → Streamer перемикає відеовхід → ELRS встановлює зв'язок) → пуск (замки, стулки) → перехід до наступного боксу. Радіо-відеочастина — спільний ресурс, пуски суворо по черзі.

2.  **Node Simulator (`node-sim`, KSIM)** — Raspberry Pi 5
    *   **Роль:** тимчасова заміна Streamer+MikroTik, поки залізо в дорозі; далі — постійний тестовий стенд. Емулює **зовнішній інтерфейс Streamer/FBI-B** (див. «Крос-проєктні контракти» → «Інтерфейс вузла»): тест-патерн відео з годинником/секундоміром, MAVLink-телеметрія віртуальних боксів, приймання CRSF-каналу керування. Сам є WireGuard-клієнтом (замість MikroTik).
    *   **Технології:** визначити в `Context_SIM.md` (Python; GStreamer/ffmpeg; pymavlink).

3.  **Ground Control Server (`server`, KSRV)** — VPS
    *   **Роль:** ядро координації: WireGuard-сервер для вузлів і робочих місць; NAT/port-forwarding по фіксованих блоках портів на вузол; ретрансляція відео у браузер без перекодування; MAVLink-роутер (телеметрія боксів ↔ команди оператора); динамічна комутація «оператор → обраний вузол»; Heartbeat/Liveness 1 Гц (RTT, джитер, втрати) для вузлів, Streamer-ів, Pico 2 і робочих місць; телеметричний хаб (клімат, комірки, струми, кінцевики); веб-інтерфейс Ground Control; автентифікація, ролі; збереження стану.
    *   **Ролі Фази 1:** Admin (усе), Tester (визначений тестовий вузол: моніторинг, відкриття боксу, активація борту, тестовий пуск). Фаза 2: Pilot (лише через QGC), Admin/Supervisor (веб), mTLS для пристроїв і користувачів, Exclusive Control Lock.
    *   **Технології:** Python 3.12 / FastAPI **у контейнері** (версію фіксує образ — див. «Еталонний стан інфраструктури»). Хостинг — Хостинг Ukraine, Shared CPU VPS 8G (4 vCPU / 8 GB / 70 GB NVMe, Київ); домен `kambala.net` (ukraine.com.ua, WHOIS privacy). ⚠ ТЗ: 16 GB / 250–500 GB NVMe, Варшава/Франкфурт/Київ — фактично взято менший VPS; достатньо, бо відео pass-through.
    *   **Пізніше (фаза не визначена в ТЗ):** внутрішній месенджер у веб-панелі; Alerting Gateway (Signal/Telegram): відкриття боксу/вібрація у черговому режимі, перегрів АКБ, зникнення PoE/зв'язку, завершення заряду.

4.  **Operator Workstation (`workstation`, KWS)** — Raspberry Pi 4, два дисплеї
    *   **Роль:** робоче місце оператора, ПЗ з нуля. При завантаженні — WireGuard до сервера, автентифікація, запит конфігурації доступних ресурсів.
    *   **Дисплей 1 — Flight Display (рішення Gans 2026-09-16: другий micro-HDMI порт, DRM `HDMI-A-2`; KWS-007):** повноекранне живе відео з мінімальною затримкою + OSD (напруга АКБ, горизонт, режим, RSSI/LQ, таймер місії, статус боксу).
    *   **Дисплей 2 (перший micro-HDMI порт, `HDMI-A-1`):** Фаза 1 — веб-інтерфейс Ground Control (браузер); Фаза 2 — Custom QGC для пілота.
    *   **Пульт оператора:** RadioMaster TX12/Boxer по USB (HID-джойстик або serial); локальний сервіс читає стіки/тумблери, формує кадри CRSF `RC_CHANNELS_PACKED` і шле по UDP на сервер → релей на `:1313` активного вузла (рішення 2026-09-11, див. C2). У штатній схемі FlyByIP пульт підключається дротом до UART/S-Port модуля **Viewer** (FlyByIP-A), який пакує CRSF в UDP для Streamer; у Kambala окремого Viewer немає — його роль виконує цей сервіс на workstation. Варіант MAVLink Manual Control (режим FBI-B `Mavlink2 → CRSF` по TCP `:1311`) відхилено: UART2 зайнятий мостом до боксів. ⚠ У `docs/technical_specification.md` (§ органи керування) лишилось старе «CRSF або MAVLink Manual Control». Рішення підтверджено 2026-09-15 (варіант C), ризики — у C4.

5.  **QGroundControl plugin (`qgc-plugin`, KQGC)** — Фаза 2
    *   **Роль:** `QGCCorePlugin`: віджети боксів («Камбала-Бокс»), кнопки активації/пуску, вибір вузла, місії; відео в QGC вимкнене (воно на Дисплеї 1). Також Фаза 2: бортова MAVLink-телеметрія з дрона (ELRS passthrough), антенний трекер, криптоаудит.

## Ролі та AI-воркфлоу
| Хто | Роль |
|---|---|
| **Gans** | Керівник. Затверджує ТЗ, тригерить L1 (`/task <ID>`), тестує залізо (RPi 4/5, вузли), дає згоду на ADLP-дії та деплої. |
| **Claude** | **L2 Architect.** Планує, пише задачі у `<P>/.agents/tasks/<ID>.md`, верифікує по `git diff`, веде Global_* і `.agents/`. Протокол: `CLAUDE.md`. |
| **Gemini (Antigravity)** | **L1 Developer.** Один воркспейс = один підпроєкт. Реалізує, тестує, оновлює Holy Trinity, комітить, дописує Report. Контракт: `<P>/.agents/AGENTS.md`. |

- **SSOT:** Holy Trinity (`Context_X / Plan_X / Changelog_X`) веде L1; Global_* веде Claude. Plan містить лише беклог.
- **Виняток «L2 fallback» (рішення Gans 2026-09-15):** коли L1 недоступний (квота/збої сервісу), Gans може разово доручити реалізацію конкретної задачі Claude: код пише субагент-виконавець у воркспейсі підпроєкту, рев'ю — окремий субагент, верифікація — прогоном; коміти від Claude з позначкою `[L2 fallback]`; факт фіксується в task-файлі. Застосовано: KWS-003 цикл 3. Протокол у l2l1-kit не змінюється.
- **Задачі:** шаблон `.agents/TASK_TEMPLATE.md`; ID — `KSRV/KWS/KSIM/KNODE/KQGC-<NNN>`; файли комітяться в git підпроєкту; закриті — у `tasks/done/`.
- Ліміти Gemini спільні з екосистемою DDL — черга задач узгоджується Архітектором.

## Крос-проєктні контракти
Зміна будь-чого тут → задачі в усі дотичні підпроєкти з `related:` (протокол Contract Sync).

### C1. Інтерфейс вузла (Streamer / FBI-B ↔ сервер) — джерело: мануал FlyByIP v1.3, ТЗ §2.1, §4.2
Це те, що `node-sim` емулює 1:1, а `server` споживає.
| Канал | Протокол | На вузлі | Примітка |
|---|---|---|---|
| Відео | **RTSP** (H.264 або MJPEG, один потік) | Streamer `rtsp://<node>:8554/video0` (також `/video1`; старі прошивки — `/video`) | Основний режим для Kambala: сервер сам забирає потік (pull). Дефолт Streamer — RTP push UDP на Viewer (:5060 H.264); у нас RTP вимкнено для економії бітрейту. Затримка кодека: MJPEG від 100 мс, H.264 від 120 мс; бітрейт H.264 від 0,5–1 Мбіт. |
| Керування ELRS | **CRSF over UDP**, порт **1313** на FBI-B | FBI-B ← пакети від оператора | Через сервер: зовнішній порт `1313N` (N = № вузла) → `1313` FBI-B. |
| Телеметрія/команди боксів | **MAVLink2** over **TCP** (FBI-B UART2 у режимі `TCP Server`, порт **1311**) → UART↔CAN → Pico 2 | FBI-B TCP :1311 | Через сервер: `1311N`. **Рішення Gans (2026-09-11): стандартний набір ArduPilot (діалект `ardupilotmega`), без власного діалекту.** Кожен бокс — окремий MAVLink-компонент (свій `component_id`, `MAV_TYPE_ONBOARD_CONTROLLER`); повідомлення: `HEARTBEAT`, `BATTERY_STATUS` (комірки, струм, температура), `SYS_STATUS`, `NAMED_VALUE_FLOAT` (клімат), `NAMED_VALUE_INT` (кінцевики, наявність дрона, стан заряду), `STATUSTEXT`; команди — `COMMAND_LONG`: `MAV_CMD_DO_SET_RELAY` (активація борту, заряд, нагрів), `MAV_CMD_DO_SET_SERVO` (замки/стулки), `COMMAND_ACK` обов'язковий. Точна карта relay/servo-індексів і NAMED_VALUE-імен — у `Context_SIM.md`, дублюється сюди при KSIM-002. |
| Конфігурація Streamer | HTTP web-інтерфейс + **REST API** (`http://<streamer>/`) | Streamer / FBI-B | **Рішення Gans (2026-09-11): перемикання відеовходу (аналог ↔ HDMI) із сервера — обов'язкове у Фазі 1.** ⛔ Блокер: мануал v1.3 лише згадує REST API, ендпоінтів не описує — потрібна документація/приклад від FlyByIP (дія Gans). До отримання node-sim реалізує власний мінімальний REST-стаб (`GET/PUT /api/video/input` → `analog | hdmi`), позначений як заміна; при появі документації — Contract Sync + parity-задача KSIM. |
| Тунель | **WireGuard**, клієнт на MikroTik RB750UPr2 (для node-sim — на самому RPi 5) | вузол → сервер | Адресація тунелю й нумерація N вузлів — у `Context_SRV.md`. |

Схема портів сервера на вузол N (ТЗ §4.2): відео `855N`, CRSF `1313N`, MAVLink `1311N`. Ліміт схеми — 9 вузлів на одну сотню/десяток; розширення нумерації вирішити при появі >9 вузлів.

Веб-конфіги реального заліза (мануал §11): Streamer `http://<streamer>:8080` (basic auth, user `voi`) — відео: кодек H.264/MJPEG, Bitrate (дефолт 2 Мбіт), I-Frames (3/10/20 за втратами), RTSP Enabled, RTP; FBI-B `http://<fbi-b>/` — Network (Remote IP), Bridge (режими `Dual` / `TCP Server` / `UDP–S-Port` / `All`), швидкості S-Port 400k, UART 460k.

### C1a. Контракт даних вузла (узгоджено з Gans 2026-09-11) — що емулює node-sim і споживає server
Сервер не має відрізняти симулятор від реального вузла. Дефолт: **6 боксів**, АКБ дрона **8S**. Реальний FBI-B прозорий → MAVLink говорять лише бокси; живість вузла — TCP-з'єднання `:1311`, RTSP, HTTP.

**Відео (`rtsp://<node>:8554/video0`, H.264, RTSP pull):** параметри як у Streamer — Bitrate (дефолт 2 Мбіт), I-Frames, опційно MJPEG (~8 Мбіт). Вхід **analog** = PAL 720×576@25, **hdmi** = Walksnail 1280×720@60. Оверлей на тест-патерні: `NODE n · ANALOG|HDMI`, годинник з мс, секундомір від старту, лічильник кадрів, **стан віртуального дрона активного боксу** (`OFF / BOOT / DISARMED / ARMED / FLYING`) і поточні канали CRSF (стіки/тумблери) — це зворотний зв'язок керування у Фазі 1, поки нема MAVLink з борту.

**MAVLink2, TCP-сервер `:1311` (сим слухає, сервер підключається).** Діалект `ardupilotmega`. `system_id` = № вузла; бокс k → `component_id = MAV_COMP_ID_USER1 + (k−1)` (25…30), `MAV_TYPE_ONBOARD_CONTROLLER`, `MAV_AUTOPILOT_INVALID`.
Періодика 1 Гц на бокс:
- `HEARTBEAT` — `custom_mode`: `0 STANDBY · 1 CHARGING · 2 READY · 3 ACTIVATED (борт під живленням) · 4 OPEN (стулки відкриті) · 5 LAUNCHED · 6 FAULT`; `system_status` STANDBY/ACTIVE/CRITICAL.
- `BATTERY_STATUS` id 0 (АКБ дрона, 8 комірок): `voltages[]` мВ, `current_battery`, `temperature`, `battery_remaining`, `charge_state`.
- `SYS_STATUS`: `voltage_battery` = шина PoE 48–54 В, `current_battery` = вхідний струм боксу.
- `NAMED_VALUE_FLOAT`: `TEMP_IN` °C, `HUM_IN` %, `TEMP_BAT` °C, `I_CHG` А.
- `NAMED_VALUE_INT` (1 Гц + миттєво при зміні): `LID` 0 закрито/1 відкрито/2 рух, `LOCK` 0 замкнено/1 відкрито, `DRONE` 0/1 (дрон у ложементі), `DRONE_PWR` 0/1, `HEAT` 0/1, `FAN` 0/1, `LID_SW_OPEN`/`LID_SW_CLOSED` 0/1 (кінцевики окремо).
- `STATUSTEXT` — зміни стану, FAULT.
Команди (`COMMAND_LONG`, `target_component` = бокс) → завжди `COMMAND_ACK`:
- `MAV_CMD_DO_SET_RELAY`: `0` = імпульс активації на бортовий ключ дрона (увімк./вимк. живлення борту), `1` = зарядка, `2` = нагрів, `3` = вентилятор, `4` = **діагностичний режим** (`DIAG`, знімає інтерлок «ARM лише при відкритих стулках» для діагностичного запуску; дзеркалиться в `NAMED_VALUE_INT DIAG` 0/1 і `STATUSTEXT` WARNING; скидається при `STANDBY`); param2 0/1.
- `MAV_CMD_DO_SET_SERVO`: `1` = замки (1000 замкнено / 2000 відкрито), `2` = стулки (1000 / 2000).
- Інтерлоки (як у Pico 2) → `MAV_RESULT_DENIED`: стулки — лише при відкритих замках; зарядка — лише при `DRONE_PWR=0`; активація — лише при `DRONE=1`; замкнути — лише при закритих стулках.
Модель: зарядка CC/CV до 4.2 В/комірку при `I_CHG` → `READY`; `TEMP_BAT` росте під зарядом; активація → `ACTIVATED`, дрон `BOOT`→`DISARMED` (~3 с); стулки відкриваються ~3 с (`LID=2`) → `OPEN`.

**CRSF over UDP, приймач `:1313`.** Приймає `RC_CHANNELS_PACKED (0x16)` від сервера: CRC, частота, втрати, 16 каналів → REST і оверлей. Віртуальний дрон активного (`ACTIVATED`) боксу реагує на канали: тумблер ARM (AUX1, ch5 > 1500) → `ARMED` лише якщо `DISARMED` і стулки `OPEN`; газ (ch3) > 1300 при `ARMED` → зліт → `FLYING`, бокс `LAUNCHED`, `DRONE=0`; ARM off у `FLYING` → «посадка поза боксом», дрон зникає. Без живлення борту (`DRONE_PWR=0`) стіки ігноруються. Зворотна телеметрія ELRS (`LINK_STATISTICS`) — **відкладено**, не в MVP. Карта каналів (ch1–4 AETR, ch5 ARM, ch6 режим) — дефолт, налаштовується.

**HTTP REST.** Емуляція двох веб-конфігів заліза (реальний API невідомий → стаб, parity-пункт): Streamer `:8080` (basic auth `voi`): `GET /api/status`, `GET|PUT /api/video {codec, bitrate, iframe_interval}`, `GET|PUT /api/video/input {"input":"analog"|"hdmi"}`; FBI-B `:80`: `GET /api/bridge` (режим, швидкості) — read-only. Окремо, **не частина емуляції** — сценарій тестів `:8090 /api/sim/...`: кількість боксів, дрон є/нема, інжект FAULT, просадка шини, скидання.

**Мережа/час:** WireGuard-клієнт до сервера (адреса в тунелі — з `Context_SRV.md`), NTP через тунель (RPi 5 без RTC-батарейки), всі сервіси — systemd, автостарт, рестарт.

**Фаза 2 (не зараз):** MAVLink від самого дрона + емуляція телеметрії автопілота (ELRS passthrough), `LINK_STATISTICS`.

**Уточнення C1/C1a за реальним залізом (2026-09-15; текст контракту вище не переписано — зміни підпроєктів через задачі з `related:`):**
- **CRSF over UDP `:1313`:** реальний FlyByIP-B у робочому для Kambala режимі **RAW UDP** приймає **сирі кадри CRSF** (дефолт C1a підтверджено), але **лише з адреси = його Remote IP/Host і з порту-джерела 1313**; вимагає **≥ 44 кадри/с безперервно** (ліміт 50 pps), пауза > 500 мс = розрив і зняття живлення TX; **повертає відповіді TX по UDP джерелу** (телеметрія лінку є вже у Фазі 1, а не 2). Штатне обрамлення (без RAW UDP) невідоме — не використовується. Деталі й журнал — C4.
- **HTTP REST Streamer:** справжній API — `http://<streamer>/api/` на порту **80** (`v3/streamer/*`, `v2/streamer/*`, `v1/ports/mavlink-router`, `viewer/*`, `settings/*`), без basic auth при вимкненому захисті; стаб `:8080 /api/status|/api/video` у C1a — вигаданий і підлягає заміні (задача KSIM на вертикаль 5). Веб-конфіг FlyByIP-B — `http://<fbi-b>/` з GET-запитами `?jconf`, `?st`, збереження через GET із параметрами.
- **Відеовходи:** реальний Streamer v1.39.0 має **два незалежні входи** (`/video0`, `/video1`), обидва v4l2/UVC, RTSP `:8554`; профіль `analog`/`hdmi` у C1a — спрощення для симулятора.
- **Міст до боксів (відкрито):** за розпіновкою модуля UART2 = S-Port (лінія ELRS TX), тож «UART2, TCP Server `:1311`» у C1 сумнівне; кандидат — MAVLink-роутер самого Streamer (`v1/ports/mavlink-router`, TCP `:5760`). Рішення після появи борта.

### C2. Сервер ↔ Workstation / браузер
- **API:** REST (JSON) для конфігурації/команд + **WebSocket** для стану в реальному часі (телеметрія боксів, heartbeat, статус вузлів). Схема ендпоінтів і повідомлень — 🔶 РІШЕННЯ, фіксується окремою задачею схеми API в `Context_SRV.md` і дублюється сюди.
- **Пріоритети Gans (2026-09-11): надійність і мінімальна затримка** — вище за універсальність і зручність розробки.
- **Відео у браузер (Дисплей 2, Фаза 1):** **MediaMTX** на сервері: RTSP pull зі Streamer → **WebRTC** у браузер (HLS — не використовувати, затримка секунди) без перекодування — узгоджується з ТЗ §4.1 «pass-through». Затверджено виходячи з пріоритету затримки.
- **Відео на Flight Display (Дисплей 1):** нативний плеєр на RPi 4 (GStreamer) тягне **RTSP напряму з MediaMTX** (без WebRTC-обгортки); OSD накладається з телеметрії по WebSocket.
- **Керування з пульта оператора — Рішення Gans (2026-09-11): CRSF over UDP.** ✅ **Підтверджено Gans 2026-09-15 (варіант C):** власне робоче місце оператора без штатного модуля FlyByIP-A; пульт по USB → сервіс workstation генерує CRSF → UDP. Ризик сумісності з реальним FlyByIP-B — див. C4. Сервіс пульта на workstation читає TX12/Boxer (USB), формує CRSF-кадри → UDP → сервер (релей з гейтом «хто керує вузлом N», без конвертації) → FBI-B `:1313`. Телеметрія лінку ELRS (RSSI/LQ) повертається тим самим CRSF-потоком. MAVLink — лише для боксів/телеметрії. У Фазі 2 QGC отримує віджети боксів (MAVLink), керування лишається в сервісі пульта. Обґрунтування: мінімум затримки і точок відмови; режим «Mavlink2→CRSF» FBI-B займає UART2 (потрібен під міст до боксів) і працює по TCP.
- **Релей керування (C2-relay, затверджено Gans 2026-09-15; реалізація — KSRV вертикалі 3+7, KSIM вертикаль 6, workstation лише конфіг).** Траса: `rc` на workstation → UDP → сервер → UDP → FlyByIP-B активного вузла; у стендовій схемі вузол = RPi 5 у ролі MikroTik (WireGuard-клієнт + маршрутизатор `wg0 ↔ eth0 192.168.13.0/24`).
  1. **Адреси й піри (фіксовані, тунель `10.66.0.0/16` — як налаштовано на VPS):** сервер `10.66.0.1`; ПК Gans `10.66.0.2`; workstation RPi 4 `10.66.0.3`; вузол-стенд RPi 5 `10.66.0.10` з `AllowedIPs 192.168.13.0/24` (реальні вузли — `10.66.0.1x`, кожен зі своєю підмережею `192.168.1x.0/24` за схемою FlyByIP). Робочі місця не маршрутизуються в підмережі вузлів; до вузла ходить лише сервер.
  2. **Workstation → сервер:** `rc` шле кадри CRSF (RC та службові, як є) на `10.66.0.1:1313` із джерела `<ws_ip>:1313`; жодних обгорток, один кадр або пара кадрів = одна датаграма, як зараз до FlyByIP-B. Сервер відповідає на ту саму адресу й порт.
  3. **Сервер → вузол:** для пари «оператор → активний вузол» сервер пересилає датаграми **без змін і без черги** на `<fbi-b>:1313` **з адреси `10.66.0.1` і порту-джерела 1313** (FlyByIP-B приймає лише від свого Remote IP і порту 1313 — C4); у зворотний бік усе, що приходить від `<fbi-b>:1313`, іде на `<ws_ip>:1313` оператора. Один сокет на вузол; кадр у кадр, без буферизації й перепакування; сервер **не вставляє власних кадрів** у потік (факт про службові кадри — C4/`Context_WS` §4).
  4. **Гейт «хто керує вузлом»:** таблиця `node_id → operator (ws_ip)`; кадри від інших адрес відкидаються з лічильником; призначення — `POST /nodes/{id}/control` (Фаза 1: сесія Admin/Tester; Фаза 2: exclusive control lock + mTLS). Без призначення сервер не шле у вузол нічого — модуль лишається у failsafe.
  5. **FlyByIP-B вузла:** Remote IP/Host = `10.66.0.1`, порт 1313, шлюз = маршрутизатор вузла (`192.168.13.10` на стенді), RAW UDP увімкнено. Streamer: RTSP забирає сервер (MediaMTX) через той самий тунель.
  6. **Вимоги до затримки/темпу:** релей додає лише мережу (≈ RTT/2 до сервера + тремтіння); має витримувати темп до 500 кадрів/с на оператора без дропів; `GET /nodes/{id}/control/stats` — pps у обидва боки, дропи, вік останнього кадру від оператора і від вузла.
  7. **Failsafe по трасі:** сервер нічого не «дотримує» — якщо кадри від workstation припинились, у вузол нічого не йде і FlyByIP-B знеструмлює TX через свій тайм-аут; безпечний кадр при втраті пульта формує сам `rc` (KWS-004).
  8. **Паритет:** `node-sim` приймає той самий потік (KSIM-003) — тести релею без заліза проти симулятора; RPi 5 як вузол-стенд конфігурується скриптом (`nftables`: у `eth0` лише від `10.66.0.1`).
- **Автентифікація Фази 1:** логін/пароль + сесійний токен; ролі Admin/Tester. Фаза 2 — mTLS для всіх пристроїв і користувачів, Exclusive Control Lock (один пілот на вузол/дрон у кожен момент).

### C3. Симулятор ↔ залізо (Sim ↔ Hardware parity)
Розбіжності node-sim від реального Streamer/FBI-B фіксуються в `Context_SIM.md` і тут. Після появи заліза — задача KSIM на звірку.

### C4. Канал керування FlyByIP — факти з мануалу IP-Video v1.3 (звірено 2026-09-15)
- **Штатна схема:** пульт → зовнішній модульний відсік → пасивний блок з XLR-роз'ємом → кабель → Viewer з модулем FlyByIP-A (стор. 43, 45; еталонний пульт у мануалі — RadioMaster TX12, External RF CRSF 400k, до 250 Гц). FlyByIP-A конвертує CRSF/SBUS з UART в UDP; FlyByIP-B у Streamer конвертує UDP в UART до ELRS TX (стор. 4). Затримка керування при прямому з'єднанні — до 5 мс (стор. 5).
- **Мережевий рівень:** модулі керування мають власні IP, окремі від Viewer/Streamer (A `192.168.13.10`, B `192.168.13.11`, у схемі з тунелем B `192.168.14.11`). Обмін — UDP `:1313` між A і B, кожен модуль шле на свій `Remote IP/Host` (стор. 19, 48). У веб-інтерфейсі модулів є вкладки Network, **Encryption**, WiFi, Ports, Bridge, Admin і статус пари «**No pair**» (стор. 44–47) — канал A↔B **спарений і, ймовірно, шифрований**. Формат UDP-пакетів у мануалі не описаний. Налаштування блоків — web-інтерфейс і **REST API** (стор. 5; сам API в мануалі не описаний).
- **ПК без Viewer (розділ 8.4, стор. 28, 31):** «Для керування за будь-яким протоколом потрібен модуль у пульт FlyByIP-A. Для керування за протоколом Mavlink можна підключати пульт безпосередньо до ПК». На схемі USB-пульт біля ноутбука підписано «**Mavlink only!**». MAVLink-шлях: FlyByIP-B, Bridge UART2 = `Mavlink2 – CRSF`, TCP Server `:1311` (стор. 32) — той самий UART2, що в Kambala відведений під міст до боксів.
- **Модуль керування (стор. 57):** UART2 = S-Port/TX2 + SBUS/RX2 + 8 В; UART0 = TX0/RX0 + 5 В; живлення XT60; габарити FlyByIP-A/B 36×55×25 мм. Другий канал керування — UART0 (стор. 26; режими Bridge `UDP – S-Port`, `All`, `Dual`).
- **Рішення Gans 2026-09-15 — варіант C.** Kambala використовує власне робоче місце оператора (workstation) без штатного модуля FlyByIP-A: пульт по USB → сервіс `rc` генерує CRSF → UDP → сервер → FlyByIP-B вузла `:1313`. Штатні варіанти A (апаратний FlyByIP-A) і B (MAVLink через UART2) відкладено. **Ризик, що лишається:** сумісність із реальним FlyByIP-B — спарювання, можливе шифрування й формат UDP `:1313` у мануалі не описані; до появи заліза сумісність не перевіряється, node-sim лише імітує канал. **Дії:** (1) запит до виробника — формат UDP `:1313`, pairing і encryption, REST API; (2) після появи реального Streamer — задача KSIM на звірку каналу керування (Sim ↔ Hardware parity) з перехопленням обміну між штатними FlyByIP-A і FlyByIP-B як еталоном; (3) запасний шлях, якщо емуляція виявиться неможливою, — варіант B з перенесенням мосту боксів з UART2 на UART0 (зміна контракту C1).
- **Наслідки:** програмна генерація CRSF → UDP `:1313` (рішення C2 від 2026-09-11, вертикаль 2 `Plan_WS`, дефолт node-sim «сирий кадр у payload») документацією не підтверджена; node-sim цей механізм лише імітує.
- **Реальний FlyByIP-B — конфіг і веб-інтерфейс** (2026-09-15; читання `?jconf` — тим самим запитом, яким сторінка сама завантажує налаштування; JS сторінки проаналізовано без запитів на зміну): Local IP `192.168.13.11`, **Remote IP/Host `192.168.13.10`, UDP-порт `1313`**; Bridge `Br='U'` = «UDP - S-PORT (2)»; S-Port/UART2 400000 бод з інверсією, PPS 150; UART0 460800 бод; FailSafe/Jitter timeout 500 мс. Порти `U=1312` (UART2) і `u=1310` (UART0) потрапляють у рядок налаштувань лише при ввімкненому TCP-режимі відповідного UART (`Off` / `TCP Server` / `TCP Server -> Remote UART`) — це TCP-порти, не UDP. **Прапорця `AES256` у конфігу немає** — найімовірніше вимкнений; поля ключа `_eA`/`_kA` модуль віддає порожніми завжди (як і ключі Wi-Fi), тож їхній вміст не читається — підтвердити візуально. Режими Bridge: `*` All, `U` UDP - S-PORT (2), `X` UDP - Serial(0) RX/TX, `S` Serial(0) RX/TX - S-PORT (2), `2` Dual Serial, `A` Dual A, `B` Uplink Backup UDP + UART(2), `D` TX diversity, `M` UDP - UART (0/2) user switched, `L` Duplicate telemetry. Debug-адресат `iD`/`pD` = `192.168.13.21:13131` (Streamer). В інтерфейсі є вбудований дамп пакетів (hex/ASCII, мітки DROP/OUT); він вмикається запитами, що змінюють маску налагодження, тому не вмикався.
- **`RAW UDP` — штатна опція FlyByIP-B.** Опис з інтерфейсу дослівно: «Transfer data over UDP without additional headers and FlyByIP control packets. Limits board to single port (S-Port or UART0) modes without Remote GPIO function». Отже **у штатному режимі UDP несе фірмові заголовки й керувальні пакети FlyByIP**, а в режимі RAW UDP — сирі дані порту. Для рішення C це кандидат на штатний шлях: FlyByIP-B у RAW UDP приймає сирі кадри CRSF від сервісу `rc`. **Ціна — один порт** (S-Port або UART0) без Remote GPIO; сумісність RAW UDP з мостом до боксів — перевірити.
- **FlyByIP-B — візуальний огляд усіх вкладок (Gans через read-only проксі, 2026-09-15).** Прошивка **UDP2S v1.18-RC10.20260811.1627**, Hw Rev 1.4, S/N **B2998**, MAC `58:2a:bd:d7:99:97`. **Encryption: AES256 ВИМКНЕНО**, ключ і Auth ID порожні — шифрування каналу немає, ризик «шифрування» з C4 знято для цієї конфігурації. Статус «**No pair**» (партнер FlyByIP-A недоступний). Network: Local `192.168.13.11/24`, Remote IP/Host `192.168.13.10`, Port `1313`, опція **«Allow incoming UDP from any remote IP/Port when remote part is hidden beyond NAT or port-mapping» = `Force port`** — модуль приймає UDP не лише від Remote IP (важливо для релею через сервер). Bridge: `UDP - S-PORT (2)`, **RAW UDP вимкнено**, FailSafe/Jitter 500 мс; S-Port(UART2) протокол Raw vs Raw(default), PPS 150, TCP Off (порт 1312); UART0 Raw vs Raw, PPS 150, TCP Off (порт 1310). S-Port/UART2: 400000 бод, 8N1, Inverted, **Single Wire (S-Port, default)**. TX Power/GPIO: TX power mode `Auto`, timeout 5 с, Power boost вимкнено, Remote power control None; IO15 = TX-U2 Pwr, IO5 = TX-U0 Pwr/RXD/U2 485EN. Admin: **Radio: auto 13.51 V** (живлення ELRS TX присутнє), Master 0.00 V, лічильники S-Port/UDP/UART0/TCP Rx/Tx/Drop = 0 (трафіку немає), є розділ **Dump packets**. WiFi: Management WiFi увімкнено (AP для налаштування), Run WiFi AP вимкнено.
- **Висновок для рішення C (2026-09-15):** шифрування вимкнене, тож єдина перешкода для власного сервісу `rc` — **фірмове обрамлення UDP у штатному режимі**. Два шляхи: (а) **увімкнути `RAW UDP` на FlyByIP-B** і слати сирі кадри CRSF на `:1313` — штатна опція, без реверс-інжинірингу; ціна — один порт на модулі; (б) відтворити обрамлення, записавши вихід FlyByIP-A через Viewer. **Розв'язання конфлікту портів:** у режимі `UDP - S-PORT (2)` модуль і так обслуговує лише S-Port, а міст до боксів можна винести з FlyByIP-B на **вбудований MAVLink-роутер Streamer** (`/api/v1/ports/mavlink-router`, UART 115200, TCP `:5760`, зараз вимкнений) — тоді RAW UDP нічого не коштує, а C1 виправляється: міст боксів = Streamer mavlink-router, а не UART2 FlyByIP-B. Потребує рішення Gans і перевірки на залізі. ⚠ Будь-який UDP-тест на `:1313` може увімкнути живлення ELRS TX (режим Auto) — перед ним антена на TX обов'язкова.
- **Тест сирого CRSF на реальному FlyByIP-B (Gans + Архітектор, 2026-09-15, скрипт `crsf_probe.py`, 50 кадрів/с `RC_CHANNELS_PACKED`, конфіг модуля не змінювався крім RAW UDP):** (1) **RAW UDP вимкнено** — сирі кадри модуль ігнорує повністю (лічильники UDP не рухаються). (2) **RAW UDP увімкнено, джерело `.33` (будь-який порт, і порт 1313)** — ігнорує. (3) **RAW UDP увімкнено, джерело `192.168.13.10:1313`** (= Remote IP модуля) — **приймає**: `UDP Rx` росте, **живлення ELRS TX вмикається** (режим Auto), але всі кадри **відкидаються на S-Port** (`S-Port Drop` = `UDP Rx`, `Tx 0`); адресний байт `0xC8`/`0xEE` не впливає. Лічильники Admin — вікно ~0,5 с, не накопичувальні. **Наслідки для архітектури:** модуль приймає UDP лише з адреси, що дорівнює його Remote IP/Host (опція «Force port» це не змінює) — для релею через сервер Remote IP модуля має бути адресою сервера в тунелі (`10.66.0.1`); порт джерела 1313 — ще не перевірено окремо. Причина Drop на S-Port з'ясовується (вбудований Dump packets).
- ✅ **Рішення C доведено на реальному залізі (2026-09-15).** Прогін 40 с на **44 кадри/с** (`crsf_probe.py 40 192.168.13.10 1313 EE 44`): кінцеве вікно `S-Port Tx 15, Drop 0`; `S-Port Rx 3 / UDP Tx 3` — **ELRS TX відповідає, модуль повертає відповідь по UDP джерелу** (зворотна телеметрія лінку працює). Dump packets: `OUT S-Port:` з нашим кадром байт у байт. Debug Log модуля: `connected 192.168.13.10, port 1313` → `E2 CRSF detected` → `S-Port TX_power 1` → `U2 CRSF detected` → `PPS request 44 for U2` → `U2 use dynamic PPS 45 vs 43` → `too far PPS adjustment … limited` → `Stabilization timeout` → після зупинки потоку `disconnected`, `S-Port TX_power 0`. Причина попередніх Drop — не формат, а **темп і фаза**: на 20 кадрів/с передавач вважає, що пульта немає (`U2: RX=0, try re-sync`), і модуль відкидає кадри до відновлення синхронізації.
- **Вимоги до сервісу `rc` (workstation) і релею (server), встановлені експериментально:** (1) FlyByIP-B у режимі **RAW UDP** (увімкнено 2026-09-15, збережено в модулі); (2) UDP-джерело = **Remote IP/Host модуля** (зараз `192.168.13.10`; для релею через сервер Remote IP модуля → адреса сервера в тунелі) і **порт-джерела 1313** — інакше пакети ігноруються без сліду; (3) стандартні кадри CRSF `RC_CHANNELS_PACKED`, адресний байт `0xEE` (`0xC8` теж приймається); (4) **темп ≥ 44 кадри/с, безперервно** — TX запитує 44 pps, ліміт модуля в RAW-режимі `UART2 PPS 50 / 20000 µs`; пауза > FailSafe 500 мс = `disconnected` і зняття живлення TX (режим Auto); (5) **низьке тремтіння** — модуль фазує кадри під опитування TX (`PPS adjustment`), `time.sleep` не годиться, потрібен монотонний таймер/планування; (6) зворотний потік: відповіді TX (телеметрія/LINK_STATISTICS) приходять по UDP на адресу і порт джерела — приймати на тому ж сокеті. Дрібне: у прогоні зафіксовано `UART0 Tx 2` — модуль щось вивів на UART0; з'ясувати при роботі з мостом боксів.
- **Наслідки для підпроєктів:** node-sim (`crsf`, вертикаль 4) — імітувати саме цю семантику: приймати лише з очікуваного джерела/порту 1313, вимагати ≥ 44 pps, FailSafe 500 мс, відповідати телеметрією на джерело; workstation (`rc`, вертикаль 2) — вимоги (3)–(6); server (вертикаль 7, релей) — зберігати порт-джерела 1313 і бути Remote IP модуля. Ризик C4 щодо сумісності власного CRSF із FlyByIP-B **знято**; лишається запит до виробника лише щодо штатного обрамлення (не критично).
- **Зворотний потік розшифровано (2026-09-15, `crsf_sweep.py 60`, борт підключений):** перший `ff` — перевірка лінії; далі ~10 кадрів/с від модуля TX, серед них `ea 0d 3a ea ee 10 00 03 7d 34 ff ff fc 18 fb` = **`RADIO_ID (0x3A)`, підтип `0x10` OpenTX SYNC**: адресат `0xEA` (пульт), джерело `0xEE` (TX); інтервал `0x00037D34` = 228 660 × 0,1 мкс = **22,87 мс (43,7 Гц)**, зсув фази `0xFFFFFC18` = −1000 × 0,1 мкс = −0,1 мс. ELRS TX сприймає `rc` як пульт і просить темп ≈ 44 Гц із фазовою корекцією — це і є джерело «PPS request 44». **Вимога до `rc` (наступна ітерація):** розбирати SYNC і підлаштовувати інтервал/фазу, як EdgeTX; модуль TX завантажується ~15 с після подачі живлення (перші відповіді з t≈15–20 с).
- ✅ **Наскрізне керування доведено на залізі (HIL PASS KWS-002, 2026-09-15):** TX12 по USB → сервіс `rc` на RPi 5 (44 Гц за SYNC модуля TX, тремтіння ~1–3 мс) → FlyByIP-B RAW UDP → ELRS TX → приймач → Betaflight: стіки рухаються, модуль пересилає 43–44 кадри/с з `Drop` ≈ 0; фізичне відключення пульта → сервіс мовчить, ARM=172, FC у failsafe; повернення → автовідновлення (зв'язок із TX через ~25 с, бо модуль знімає живлення TX після 500 мс тиші). **Рішення Gans:** при втраті пульта `rc` має слати безпечний кадр (стіки в центрі, газ на останньому значенні, перемикачі як були) замість мовчання; тайм-аут утримання — відкрите питання; карта ARM налаштовується пізніше.
- **HIL меню модуля TX через `rc` (KWS-003, 2026-09-15, RPi 5 ↔ FlyByIP-B ↔ ELRS TX; докази — пасивний перехоплювач на `eth0` і телеметрія):** протокол параметрів CRSF проходить через FlyByIP-B у RAW UDP в обидва боки повністю (PING/DEVICE_INFO/READ/ENTRY з chunk-ами/WRITE); дерево модуля читається цілком; **Switch Mode = Wide** (вимога C5 для ch13/ch14 виконана). Факти про модуль: (1) **поля `Bind` у дереві немає** — прошивка з bind phrase, ELRS його ховає; бінд викликається службовим кадром `COMMAND 0x32 [0x10][0x01]` (як EdgeTX); (2) на `WRITE` модуль не відповідає, а `READ`, надісланий одразу за `WRITE`, повертає старе значення або губить запис — потрібна пауза ≥ 200 мс і перечитування з повтором (так робить Lua ELRS); (3) `ELRS_STATUS 0x2E` модуль віддає лише у відповідь на `WRITE` з індексом поля 0; (4) записаний `Max Power` **не пережив перезапуск `rc`** (модуль знеструмлюється FlyByIP-B через 500 мс тиші) — відкрите питання: чи потрібен `MODEL_SELECT_ID` від пульта для commit, чи модуль не зберігає; (5) команди (`Update RX/TX`, `Enable WiFi`…) виконуються одним `WRITE [id][1]` — UI зобов'язаний вимагати підтвердження. Цикли 3/3b виконано в режимі L2 fallback (`bab5be9`, `a98bae0`, `d0e2ebc`); **HIL PASS 2026-09-15, KWS-003 done.** Додаткові факти з HIL: модуль з прошивкою **MilELRS** — Packet Rate лише 14/25/44/82 Гц; `Max Power` **не зберігається** після знеструмлення (Packet Rate зберігається), `MODEL_SELECT_ID` не допомагає → параметри лінку зберігає профіль борта і `rc` застосовує їх після кожного старту модуля; FlyByIP-B знімає живлення з TX через 5 с без UDP (тому мовчання `rc` при втраті пульта = перезавантаження TX і 60–75 с до відновлення — аргумент за безпечний кадр KWS-004); RX у Wi-Fi-режим через 60 с без лінку після власного перезавантаження; **USB-розриви TX12 корелюють із потужністю TX** (2000 мВт — 12 за 10 хв, 25 мВт — 0 за 5 хв) — на стенді тримати 25 мВт, на РМ пульт подалі від передавача; `rc` на 82 Гц слідує SYNC автоматично.
- **Затримка керування виміряна (2026-09-15, стенд RPi 5 → FlyByIP-B RAW UDP → ELRS TX 44 Гц, Wide → RX → Betaflight; метод: RPi перекидає канал у наступному кадрі за UDP-командою з ПК, ПК опитує польотник по MSP `MSP_RC` через USB, ~16 мс на опитування, Wi-Fi-плече віднято за RTT; батарея відключена; скрипти `lat_pi.py`/`lat_pc.py`):** **стік (ch1)** — n=30: мін 45, медіана 74, p90 100, макс 162 мс (з урахуванням квантування MSP реальна медіана ≈ 65 мс); **тумблер (AUX3 = ch7)** — мін 81, медіана 123, p90 165 мс, асиметрія за напрямком 150/95 мс, бо в режимі Wide допоміжні канали ch6–ch16 передаються по черзі по одному на пакет. Не включено плече «пульт → `rc`» (HID + очікування такту, у середньому ~10 мс) — повний шлях оператор → FC ≈ 85 мс у медіані. **Прогін фази** (зсув нашого UDP-кадру 0…21 мс відносно SYNC, по 12 замірів): медіани 74–96 мс без мінімуму — фаза не важіль, FlyByIP-B перефазовує кадри сам. **82 Гц** (максимум цього модуля: Packet Rate лише 14/25/44/82 Гц, форк 900 МГц): стік — мін 45, медіана 52, p90 75, макс 78 мс. Підлога ~45 мс не залежить від темпу — фіксована частина FBI-B + TX + RX + FC (+ ~16 мс квантування MSP); розкласти її по ланках без осцилографа/Blackbox неможливо. **Висновок:** на цьому модулі найкраще ≈ 45–50 мс медіана (82 Гц, −3 дБ чутливості); вимога Gans «≤ 20 мс» досяжна лише з іншим модулем (2,4 ГГц ELRS 250–500 Гц) і, ймовірно, без FlyByIP-B у ланцюжку керування — рішення по залізу. **Рішення Gans 2026-09-15:** модуль лишено на **82 Гц**; наступного дня — інший модуль TX на пробу (заміряти тими ж скриптами). Розклад підлоги (середні, 82 Гц): такт `rc` ~6 · перефазування FlyByIP-B ~6–9 · черга ефірного пакета TX ~6 · час у повітрі LoRa 900 МГц ~8 · RX→FC ~2–3 · квантування MSP ~8 ≈ 40–45 мс; три ланки з власним тактом дають більшість. Наслідки: (1) для профілю борта Packet Rate — головний важіль (100 Гц ≈ удвічі менше), рішення за Gans; (2) **функції на тумблерах (зокрема ch13/ch14 навантаження) мають затримку до ~180 мс і команду треба тримати ≥ 200 мс**; критичне за часом — лише стіки і ch5; (3) через сервер додасться ≈ RTT/2 тунелю.
- **Службові кадри ELRS через FlyByIP-B (HIL KWS-003 цикл 3b, 2026-09-15):** лічильники модуля `?st` показують `UDP Rx 43 → S-Port Tx 43, Drop 0`, зворотно `11 → 11, Drop 0`, `UART2 PPS limit 210, 22866 µs, in-jit 399 µs` — модуль нічого не викидає; датаграми відповідей цілі (435/435). Втрати READ (34 % для chunk 0, ~10 % для наступних) і статус-запитів (2–20 %) відбуваються **всередині модуля TX** (не відповідає), корелюють із довжиною відповіді; режим `replace` (кадр замість RC-кадру) проти `separate` різниці по втратах не дав, а суб'єктивно збільшив затримку керування (Gans) — дефолт повернуто на `separate`. Причина — відкрите питання (кандидат: колізії на напівдуплексному S-Port; перевіряти через Dump packets модуля).
- ⚠ **Можлива помилка в контракті C1:** за розпіновкою модуля (стор. 57) **UART2 — це S-Port**, лінія до ELRS TX; UART0 — окремий порт. C1 розміщує міст до боксів на «UART2 у режимі TCP Server :1311», тобто на лінії ELRS; найімовірніше, міст має бути на **UART0**. Від цього залежать обґрунтування відхилення варіанта B (режим `Mavlink2 → CRSF` виводить CRSF саме на UART2/S-Port, тож конфлікту з мостом на UART0 могло не бути) і сумісність RAW UDP з мостом. Потребує рішення Gans і виправлення C1 за протоколом Contract Sync.
- **Реальний Streamer:** веб-інтерфейс «VideoOverIP» — nginx 1.18.0 на **`:80`** (не `:8080`, як у мануалі v1.3; `:8080` закритий); також відкриті `:22` (SSH) і `:8554` (RTSP). Для node-sim — parity-пункт: симулятор емулює веб-конфіг Streamer на `:8080`.

### C5. Карта каналів пульта оператора (затверджено Gans 2026-09-15)
Стандарт Kambala для 16 каналів CRSF, які генерує сервіс `rc` (workstation) і споживає польотний контролер вузла. Номер каналу = позиція в `RC_CHANNELS_PACKED`; у Betaflight `chN` = `AUX(N−4)`. Значення: 2-позиційний — `172`/`1811`, 3-позиційний — `172`/`992`/`1811`, пропорційний — `172…1811`. Прив'язка каналу до конкретного органа TX12 — у конфігурації `rc` (`KWS_RC_CHANNEL_MAP`, згодом майстер калібрування у веб-UI); призначення на боці FC (Modes, servo mixer, VTX-таблиця) — зона борта.

| Канал | Функція (ім'я в профілі борта) | Призначення | Орган | FC | Примітка |
|---|---|---|---|---|---|
| ch1–ch4 | `roll` `pitch` `throttle` `yaw` | крен / тангаж / газ / рискання | стіки, AETR | AETR1234 | підтверджено на залізі (`a0…a3`) |
| ch5 | `arm` | **ARM** | 2-поз. тумблер (`a4`) | AUX1 → ARM 1750–2100 | єдине місце для ARM: в ELRS ch5 іде в кожному пакеті; інверсія в `rc` заборонена |
| ch6 | `flight_mode` | режим польоту | 3-поз. тумблер (`a5`) | AUX2 → ANGLE / ACRO / LAZYFLY | вже призначено в Betaflight |
| ch7 | `vtx_power` | потужність VTX | 3-поз. | AUX3 → VTX-таблиця | 25 мВт у боксі / середня / максимум у польоті; спосіб — на боці FC |
| ch8 | `servo1` | серво 1 (на борті) | потенціометр або 3-поз. | AUX4 → servo mixer | пропорційно або 3 фіксовані положення |
| ch9 | `servo2` | серво 2 (на борті) | потенціометр або 3-поз. | AUX5 → servo mixer | те саме |
| ch10 | `user1` | USER1 | тумблер | AUX6 → USER1 | вже призначено в Betaflight |
| ch11 | `user2` | USER2 | тумблер | AUX7 → USER2 | вже призначено в Betaflight |
| ch12 | `cruise_mode` | **режим «круїз»** | 2-поз. тумблер | AUX8 | реалізація режиму на боці FC — окремо |
| ch13 | `payload_arm` | **ARM корисного навантаження** | 2-поз. тумблер | AUX9 | запобіжник: без `1811` тут ch14 не діє |
| ch14 | `payload_act` | **активація корисного навантаження** | момент. кнопка / тумблер | AUX10 | діє лише при ch13 = `1811` |
| ch15–ch16 | — | резерв | — | AUX11–AUX12 | `992` |

- **Вимога до лінку ELRS:** режим перемикачів TX має передавати **всі** канали ch6–ch16 (`Switch Mode: Wide` або повнорозрядний режим); у режимі `Hybrid` канали ch13–ch16 не передаються, і ch13/ch14 (навантаження) не працюватимуть. Перевіряти в меню модуля TX (KWS-003) і зафіксувати в `Context_WS.md`.
- **Безпечний кадр `rc` при втраті пульта (рішення Gans 2026-09-15):** ch1/ch2/ch4 = `992`, ch3 = останнє значення, усі перемикачі (ch5–ch16) — як були до втрати; тримати **до відновлення зв'язку з пультом**, без тайм-ауту. Логіка «навантаження» (ch13 → ch14) виконується на борті й не залежить від `rc`.
- ✅ **HIL PASS KWS-004 (2026-09-16):** безпечний кадр при втраті пульта (кадри йдуть, газ/ARM утримано, TX не знеструмлюється), профілі пульта/борта, майстер калібрування і прив'язка функцій працюють на залізі; Betaflight бачить AUX3–AUX8 без перезапуску. Пресет Gans: VTX→T3, серво1→SD, серво2→SC, USER1/2→T1/T2, круїз→T4. Обмеження EdgeTX USB Joystick: 8 осей = CH1–CH8 мікшера, решта — кнопки (потенціометри на CH9+ приходять двійково) — модель EdgeTX «Kambala» на кінець Фази 2.
- **Наслідки для підпроєктів:** workstation — наступна ітерація `rc` (карта ch7–ch14 як дефолт, безпечний кадр, майстер калібрування); node-sim (`crsf`, віртуальний дрон) — відображати ch1–ch14 за цією таблицею; борт — призначити AUX3, AUX8–AUX10 у Betaflight, servo mixer, VTX-таблицю (зона Gans).

**Архітектурне рішення Gans 2026-09-15 — профілі.** Таблиця вище — це **дефолтний профіль борта** (`kambala-default`), а не константа системи. Дві незалежні сутності:
- **Профіль борта (модель дрона):** семантична розкладка каналів (яка функція на якому `chN`, тип органа, значення), параметри зв'язку (ELRS Packet Rate/Switch Mode/Telemetry Ratio), відео (вхід Streamer, кодек/бітрейт), телеметрія (що показує OSD/Ground Control). Обирається оператором/сервером для конкретного вузла; зміна моделі змінює всі чотири групи разом.
- **Профіль пульта (модель пульта, пресет):** фізична відповідність органів HID-пристрою (осі/кнопки, напрямок, діапазони, тип тумблера) **логічним органам** (стік A/E/T/R, тумблери SA…SF, повзунки S1/S2, кнопки). Готові пресети (перший — `tx12-joystick`), майстер калібрування створює або править пресет.
- **Зв'язок — не лише ELRS (рішення Gans 2026-09-15).** Розділ `link` профілю борта описує *тип* радіолінку і його драйвер: `elrs` (модуль TX керується протоколом параметрів CRSF через `rc`, KWS-003), **`sine.link`** (точно буде; керування TX — через окрему програму виробника, у Kambala — заглушка до Фази 2), інші — за появи. Меню модуля у веб-UI `rc` — це драйвер `elrs`, а не загальна властивість системи; UI показує меню лише для активного драйвера, для інших — заглушку «керування у зовнішній програмі». Реалізація не-ELRS драйверів — Фаза 2.
- `rc` будує CRSF-канали як композицію: *профіль борта* (функція → канал) × *профіль пульта* (орган → вісь HID) × *прив'язка* (функція → орган, зберігається в профілі борта як дефолт і може бути перевизначена оператором).
- **SSOT профілів — сервер** (Ground Control, Фаза 1, вертикаль сервера на сесію/конфіг): workstation отримує активний профіль борта разом із сесією і кешує; профіль пульта — локальний для РМ. До появи цього на сервері workstation тримає обидва профілі у локальних JSON-файлах того самого формату (KWS-004), щоб перенести без переробки. Формат файлів фіксується в `Context_WS.md` і після появи сервера — тут як контракт C2.

---

## Еталонний стан інфраструктури (звірено 2026-09-11)

Факти зібрано Архітектором read-only по SSH. Оновлювати при кожній зміні інфраструктури — інакше наступна сесія починає з нуля (саме так і сталося 2026-09-11: VPS налаштували 2026-09-10, у SSOT не записали, довелося відновлювати інвентаризацією).

### Мережа — WireGuard
| Параметр | Значення |
|---|---|
| Сервер (`wg0`) | `10.66.0.1`, слухає `:51820/udp`, публічний ключ `P3UXN/WTthgvYw1V0mOPJ7wE1z0IYfcs5FIUS8OINmI=` |
| Підмережа тунелю | `10.66.0.0/16` |
| Пір 1 — ПК Gans | `10.66.0.2/32`, тунель `gans-pc` (служба `WireGuardTunnel$gans-pc`) |
| Інші піри | **немає** — node-sim (RPi 5) і workstation (RPi 4) у тунель ще не додані |

### VPS `kambala.net`
| Параметр | Значення |
|---|---|
| Публічна адреса | `173.242.48.210` (A), `2a00:7a60:0:30d2::2` (AAAA) |
| ОС | **Ubuntu 26.04.1 LTS**, `x86_64` |
| Системний Python | **3.14.4** — на Ground Control не впливає: сервіс у контейнері, версію фіксує образ (рішення 2026-09-14) |
| Доступ | SSH під `root` **лише через тунель** (`root@10.66.0.1`). Ззовні порт 22 закритий на рівні хостингу: `iptables INPUT` порожній, `ufw inactive`, при цьому `173.242.48.210:22` недоступний, а `:443` доступний. `sshd` слухає `0.0.0.0:22` лише по IPv4 (`[::]:22` немає) |
| Публічні порти | `80`, `443` (TCP і UDP/QUIC) — усі через Caddy |

### Розгорнутий стек — `/opt/kambala` (власник `gans`, docker compose)
| Контейнер | Образ | Роль |
|---|---|---|
| `caddy` | `caddy:2-alpine` | реверс-проксі, TLS, єдина публічна точка входу (80/443) |
| `postgres` | `postgres:17-alpine` | СУБД, внутрішній `5432` |
| `grafana` | `grafana/grafana-oss:latest` | моніторинг, внутрішній `3000` |
| `prometheus` | `prom/prometheus:latest` | метрики, внутрішній `9090` |
| `node-exporter` | `prom/node-exporter:latest` | метрики хоста, внутрішній `9100` |

Склад каталогу: `compose.yml`, `.env` (права `600`, секрети — не читати й не копіювати в репозиторій), `.env.example`, `caddy/`, `grafana/`, `prometheus/`, `site/`.

**Інвентаризація для взяття під git (2026-09-15, read-only).** У `/opt/kambala` лише конфіги (~260 КБ): `compose.yml`, `.env.example`, `caddy/Caddyfile`, `prometheus/prometheus.yml`, `grafana/provisioning/datasources/prometheus.yml`, `site/` (статика, 240 КБ). Секретів у файлах немає: обидва паролі в `compose.yml` (`POSTGRES_PASSWORD`, `GF_SECURITY_ADMIN_PASSWORD`) — через підстановку `${…}` з `.env`; у решті конфігів збігів `password/secret/token` 0. **Дані лежать не в каталозі, а в Docker-томах** `kambala_caddy_data` (TLS-сертифікати), `kambala_caddy_config`, `kambala_pg_data`, `kambala_prom_data`, `kambala_grafana_data` — git їх не збереже, потрібен окремий бекап. Фактичні версії під плаваючими тегами (для закріплення): `caddy` v2.11.4 (`2-alpine`), `postgres` 17.11 (`17-alpine`), `prometheus` 3.14.0 (`latest`), `node-exporter` 1.12.1 (`latest`), `grafana-oss` 13.0.2 (`latest`). **Точні дайджести робочих образів** (мультиархітектурні індекси, звірено з Docker Hub 2026-09-15): `caddy@sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648`, `postgres@sha256:18cfe3ef5e6815560c98237d6216d1e5119702fb0f3894c8785dd58b8bbe5d73`, `prom/prometheus@sha256:5ce7540c3c00ef4ab0c9d2c995c6a5b9c421f44b4a115d97a2c7af3b1c21cbb0`, `prom/node-exporter@sha256:1b4e4438faca4dd7e001dd445d161a4a2091b0fededa84093b3a8dfeae1f1be0`, `grafana/grafana-oss@sha256:5dad0df181cb644a14e13617b913b261a54f7d4fd4510721dba420929f35bea2`. ⚠ Теги з точними версіями (`2.11.4-alpine`, `17.11-alpine`, `v3.14.0`, `v1.12.1`, `13.0.2`) у реєстрі існують, але **вже вказують на інші збірки** — дайджести всіх п'яти не збігаються з робочими (теги перезібрано після розгортання). Робочі індекси реєстр досі віддає (HTTP 200). Отже закріплювати лише за тегом не можна — наступний `pull` підмінить образи; закріплювати як `образ:версія@sha256:<дайджест>`. Ризик: реєстр може з часом прибрати маніфести без тегів — варіант страховки `docker save` робочих образів. На VPS є `git`, `rsync`, `tar`; вільно 59 ГБ.

✅ **Конфігурацію `/opt/kambala` взято під git** (KSRV-002, 2026-09-15): `github.com/pe1ryxa-ctrl/kambala-server`, каталог `infra/`; образи закріплені як `тег@sha256` робочих дайджестів, `infra/README.md` описує ручне розгортання. ✅ Закріплений `compose.yml` **розгорнуто на VPS 2026-09-15** за командою Gans (бекап `compose.yml.bak-20260914-103758` поруч): `docker compose config` OK, `up -d` перестворив 5 контейнерів на тих самих дайджестах, `https://kambala.net` 200/TLS OK. `/opt/kambala` на VPS і далі не git-репозиторій — джерело правди тепер `server/infra/`, зміни йдуть звідти вручну за README. Дані в Docker-томах git не зберігає — бекап окремим рішенням.

**Архітектурні рішення для Ground Control Server (затверджено Gans 2026-09-14):** (1) Ground Control працює **контейнером у наявному `compose.yml`** за Caddy (TLS уже вирішено), з **окремою БД і користувачем** у наявному Postgres, віддає `/metrics` у наявний Prometheus; версію Python фіксує образ, системний Python хоста значення не має. (2) **MediaMTX — окремий контейнер у тому ж compose** (вибір MediaMTX — C2). Деталізація — `Context_SRV.md` (KSRV-001). Відкрите питання до вертикалей WireGuard і відео: доступність вузлів у тунелі з bridge-мережі Docker (`wg0` живе на хості).

### Бойова траса керування через релей — стан 2026-09-16 (HIL KSIM-004 / KSRV-003)
| Елемент | Стан |
|---|---|
| VPS `wg0` | піри: ПК `10.66.0.2`, RPi 4 `10.66.0.3`, RPi 5 `10.66.0.10` + `192.168.13.0/24`; конфіг рендериться `infra/wireguard/apply.sh` з `/etc/kambala/wireguard.keys`; `MTU 1380`; маршрут `192.168.13.0/24 dev wg0`. Код `server` на VPS — `/opt/kambala/src` (архів з `origin/main`), compose `/opt/kambala/compose.yml` з `build.context: /opt/kambala/src` |
| VPS firewall | `nftables` `inet filter input policy drop`: додано `iifname wg0 udp dport 1313 accept` **перед** SSH-ratelimit (`limit rate over 6/minute`) — інакше односпрямований UDP (завжди `ct new`) обмежувався до 6 пакетів/хв; правило і в `/etc/nftables.conf` |
| Релей | контейнер `relay` (host network), сокет `10.66.0.1:1313`, API `127.0.0.1:8001`; `KSRV_NODES=sim=192.168.13.11`; оператор `10.66.0.3` призначений вузлу `sim` |
| RPi 5 (вузол) | `deploy/wg-node/`: `wg0 10.66.0.10/16`, forwarding, `nftables` (у `eth0` лише від `10.66.0.1`, з `eth0` лише до `10.66.0.1`), timesyncd на `10.66.0.1`; RPi 4 до FlyByIP-B не дістає (перевірено) |
| FlyByIP-B | Remote IP/Host `10.66.0.1`, Gateway `192.168.13.10`, port 1313, RAW UDP — прямий шлях зі стенду більше не працює (модуль приймає лише від сервера) |
| RPi 4 (РМ) | `~/kws` бандл `7b92d19` (KWS-006: `profiles/active.json`, legacy-банер) + venv (`python-dotenv`), `.env`: ціль `10.66.0.1:1313`, джерело `10.66.0.3:1313`; TX12 по USB; `rc` у стані `link`, слідує SYNC модуля (82 Гц) |
| Відео | ✅ **2026-09-16:** MediaMTX на VPS (`node/sim` ← `rtsp://192.168.13.21:8554/video0`, **`rtspTransport: tcp`** — по UDP через SNAT не проходить; `sourceOnDemand`), WebRTC у браузері ПК через тунель `http://10.66.0.1:8889/node/sim` — картинка з FPV-камери борта з OSD Betaflight. На RPi 5 для Streamer додано SNAT `10.66.0.1→192.168.13.10` лише для `daddr .21` (Streamer без шлюзу; FlyByIP-B не чіпається). Firewall VPS: `wg0` tcp 8554/8889, udp 8189. Хвости KSRV-004: джерело `node/sim` у репо вказує на `.11` (треба `.21`/окрема змінна), `KSRV_NODE_VIDEO` не передається в контейнер `relay`, `rtspTransport: tcp` у шаблон; node-sim RTSP (KSIM-005) — шлях `node/sim-video` ← `rtsp://10.66.0.10:8554/video0` |
| Flight Display (RPi 4, прототип) | ✅ **2026-09-16:** монітор на HDMI-2 (HDMI-1 «disconnected»); `~/kws/flight-display.sh` (не в репо) — `gst-launch-1.0 rtspsrc rtsp://10.66.0.1:8554/node/sim protocols=tcp latency=0 drop-on-latency=true ! rtph264depay ! h264parse ! v4l2h264dec ! videoconvert ! waylandsink fullscreen=true sync=false` у сесії `wayland-0` (labwc): **7 % CPU**, затримка на око така сама, як у WebRTC у браузері. `ffplay` (програмний декодер, `-fflags nobuffer`) — 64 % CPU і помітно більша затримка; `ffplay -codec:v h264_v4l2m2m` — 250 % CPU (конвертація yuv420p→bgr24 на CPU). Пакети: `gstreamer1.0-{tools,plugins-base,plugins-good,plugins-bad,libav}`. Висновок для задачі KWS «Flight Display»: тільки GStreamer + `v4l2h264dec`, `latency=0`, `sync=false` |
| Результат | ✅ **HIL PASS 2026-09-16:** TX12 → RPi 4 → Wi-Fi → VPS → RPi 5 → FlyByIP-B → ELRS → приймач: модуль приймає 83 кадри/с, повертає відповіді, релей віддає їх оператору; `rc` на RPi 4 у `link`, LQ 100 %; Betaflight бачить канали з RPi 4. **Затримка через релей (82 Гц, стік):** мін 60 / медіана 67 / p90 77 / макс 90 мс проти прямої траси 38/51/62/78 — релей додає ≈ один RTT тунелю (~16 мс). MilELRS: приймач захоплює лінк лише на 44 Гц, далі темп піднімається на льоту |
| Happymodel 915 (стандартний ExpressLRS 4.1 FCC915, bind phrase спільна з RX другого борта Betaflight BLADE_F7) | Дерево 31 параметр, є `Bind`; Packet Rate `D50/25/50/100/100 Full/200`; `rc` слідує SYNC 5,0 мс (200 Гц). **FlyByIP-B: PPS limit 201** — на 200 Гц `rc` через тунель дає ~208 pps із тремтінням 4,6 мс → модуль викидає ~13 кадрів/с (6 %). **Затримка через релей (стік, борт по USB):** 200 Гц — мін 34 / медіана **47** / p90 64 / макс 105 мс; 100 Гц Full — 45 / 61 / 66 / 124 (гірше: довший інтервал коштує більше, ніж дропи). Лишено 200 Гц. Приймач після ввімкнення без TX іде у Wi-Fi через 60 с — перезавантажити борт, коли TX уже в ефірі |

### Тестовий стенд — Raspberry Pi 5 (`node-sim`)
| Параметр | Значення |
|---|---|
| Хост | `kambala-sim.local` (mDNS), користувач `gans`, Debian 13 Trixie `aarch64`; деплой — `node-sim/deploy/push.ps1` (перевірено HIL 2026-09-11) |
| WireGuard | у тунель ще не доданий |
| **Живлення — придатне** | 2026-09-14, четвертий БЖ (чинний). Від старту `throttled=0x0`, жодного `Undervoltage` у `dmesg` ні на старті, ні під навантаженням. `EXT5V_V` у спокої 5,06 В; після 2 хв роботи 90 с повного навантаження 4 ядер — мінімум **4,80 В**, 0 із 30 замірів з активним тротлінгом, температура до 59 °C; запас над порогом ~170 мВ. USB-PD досі заявляє **3000 мА** (`usb_max_current_enable=0`, USB-порти до 600 мА) — для headless-плати на Wi-Fi без USB-пристроїв неістотно. Для порівняння, того ж дня: БЖ №2 — 4,62 В з активним тротлінгом; БЖ №3 — 4,72 В і просадки на старті. Вертикаль 2 `Plan_SIM` розблоковано; `vcgencmd get_throttled` під час HIL-замірів відео все одно фіксувати (заміри з активними бітами недійсні) |
| **USB на стенді — нестабільний** | 2026-09-15: TX12 (`1209:4f54`, HID Gamepad, живиться/заряджається по USB-C) на USB RPi 5 **перепідключався 11 разів за ~3 год** пачками по 2 за секунду кожні 60–90 с (dmesg `usb 3-1: USB disconnect` → `new full-speed`), `throttled=0x0`. Імовірна причина — ліміт струму USB-портів 600 мА при БЖ 3 А (`usb_max_current_enable=0`): зарядка TX12 перевищує ліміт, порт скидається. Наслідок для ПЗ: HIL KWS-002 виявив, що `rc` при втраті пристрою заморожує канали з ARM у верхньому положенні (виправляється в KWS-002). Варіанти: `usb_max_current_enable=1` у `/boot/firmware/config.txt` (до 1,6 А на порти; при 3 А БЖ прийнятно, перевірити `get_throttled`), або живлений USB-хаб, або живити TX12 окремо. Для RPi 4 робочого місця — врахувати те саме (офіційний БЖ 5,1 В / 3 А, порти до 1,2 А) |
| **Карта каналів TX12 (Joystick-режим), підтверджена на залізі 2026-09-15** | `KWS_RC_CHANNEL_MAP=ch1=a0,ch2=a1,ch3=a2,ch4=a3,ch5=a4,ch6=a5` — стіки AETR = осі 0–3; **тумблери в USB-Joystick EdgeTX є осями** (a4–a7, у спокої −32767), не кнопками: ARM = `a4` (безпечне положення → 172, Betaflight AUX1 1000; бойове → 1811, AUX1 2000, режим ARM 1750–2100), режим = `a5` (AUX2). Стара карта `ch5=b3, ch6=a4` була хибною (ARM з випадкової кнопки). Розриви USB (`error -71`, 39 подій/день) — механічні, кабель/роз'єм; вимога до заліза РМ: фіксований кабель або кутовий USB-C із защіпкою. Майстер калібрування у веб-UI РМ — вимога Gans |

### Робоче місце оператора — Raspberry Pi 4 (`workstation`)
| Параметр | Значення |
|---|---|
| Хост | `kambala-ws.local` (mDNS), користувач `gans`, SSH лише за ключем (той самий ключ, що для RPi 5); прошито 2026-09-15 через Raspberry Pi Imager 2.0.11 |
| Плата / ОС | Raspberry Pi 4 Model B Rev 1.5, 4 GB; Raspberry Pi OS 64-bit з робочим столом, Debian 13 Trixie, Python 3.13.5, `graphical.target`, KMS (`vc4-kms-v3d`, `max_framebuffers=2`) |
| Локаль | часовий пояс `Etc/UTC`, клавіатура `us`; Wi-Fi `Garage_service` (2026-09-15 адреса `192.168.50.127`, DHCP) |
| Стан на першому старті | `throttled=0x0`, 39 °C, диск 29 ГБ (20 % зайнято); обидва HDMI `disconnected` — моніторів, клавіатури й миші ще немає (монітори очікуються, клавіатура/миша — наступного дня) |
| Далі | `sudo` без пароля для `gans`, енергозбереження Wi-Fi вимкнути, WireGuard-пір, деплой `workstation`; HIL задач `rc` переносити сюди з RPi 5 |

### Реальне залізо вузла
- **FlyByIP IP-Video Streamer (з модулем керування FlyByIP-B) — у наявності** (повідомлено Gans 2026-09-15). Комплектація (Viewer / FlyByIP-A, ELRS TX, VRX, MikroTik), версія прошивки, мережеві адреси й стан підключення — уточнюються. Відкриває раннє зняття ризику C4 ще до появи всієї системи: огляд веб-інтерфейсу FlyByIP-B (Encryption, Ports, Bridge, Remote IP), перевірку REST API і реакції FlyByIP-B на UDP `:1313`; це також привід для задачі Sim ↔ Hardware parity (CLAUDE.md §6).
- **Перше спостереження (2026-09-15, пасивне прослуховування `eth0` RPi 5 протягом 20 с, без змін налаштувань):** Streamer підключено кабелем до `eth0` RPi 5, лінк 100 Мбіт/с full duplex; IPv4 на `eth0` RPi немає (Streamer DHCP не роздає). У мережі лише заводські адреси режиму прямого кабелю (мануал, стор. 48): **FlyByIP-B** `192.168.13.11` (MAC `58:2a:bd:d7:99:97`) ~1 раз/с шукає ARP-ом `192.168.13.10` — свого партнера **FlyByIP-A**, тобто його Remote IP = `.10`; **Streamer** `192.168.13.21` (MAC `88:a2:9e:8e:91:e7`) шукає `192.168.13.20` (Viewer, ціль RTP `:5060`) і `192.168.13.10`. UDP-трафіку немає — партнерів не знайдено. Комплект повний (є Viewer з FlyByIP-A), Viewer на момент спостереження вимкнений. Вільна адреса для стенду в цій підмережі — `192.168.13.33` (рекомендована мануалом для ПК, стор. 46; ARP-запитів на неї немає).
- **Веб-інтерфейс Streamer (огляд Gans через SSH-тунель, 2026-09-15):** «VideoOverIP», прошивка **Streamer v1.39.0**, S/N `8E91E7`; меню Network → Dashboard, Interface Settings, Starlink, VPN, Speedtest, Debug; у Settings — **Password protection for web interface: вимкнено**, розділ **Change Port** показує значення **`8080`**, хоча інтерфейс фактично відповідає на `:80`, а `:8080` закритий — налаштування розходиться з реальним портом (імовірно, nginx слухає `:80` незалежно від цього поля); також є System Logs → Download і **Reset System**. **Security baseline — рішення Gans 2026-09-15: ізоляція на мережевому рівні замість пароля.** Об'єкт охороняється, фізичного доступу до LAN вузла немає. Особа піра в тунелі = його WireGuard-ключ → фіксована адреса (`AllowedIPs` на сервері), підробити неможливо. **MikroTik вузла** — шлюз: firewall на інтерфейсі `wg` приймає лише від дозволених адрес тунелю (за замовчуванням **лише сервер `10.66.0.1`**, бо робочі місця ходять до вузла тільки через сервер: MediaMTX, CRSF-релей, Ground Control), решту drop; керування MikroTik — так само. Адреси конкретних РМ додаються лише за потреби прямого шляху. **Сервер (вертикаль 3):** фіксовані адреси пірів, робочі місця не маршрутизуються в підмережі вузлів. **node-sim (вертикаль 6):** RPi 5 відтворює правило локальним `nftables` на `wg0`. Пароль веб-інтерфейсу Streamer **не вмикається**; **SSH `:22` Streamer** (перевірено 2026-09-15 запитом методів, без входу): `OpenSSH 8.4p1 Debian 11`, для всіх користувачів **лише `publickey`**, пароля немає — заводських облікових даних не існує, доступ лише з ключем виробника. Ризик знято; керування Streamer із сервера — через REST API. Провізіонування SSH при активації вузла можливе лише після того, як FlyByIP встановить наш ключ (запит до виробника за потреби). FlyByIP-B через той самий тунель у звичайному вікні Chrome відповідає «Header fields are too long» — вбудований веб-сервер модуля не вміщує заголовки з cookies `localhost`; відкривати в режимі інкогніто.
- **REST API Streamer v1.39.0 — реальний, прочитано GET-запитами (2026-09-15).** База **`http://<streamer>/api/`** (axios `baseURL='./api/'`), без автентифікації при вимкненому захисті. Читання: `v3/streamer/configs`, `v3/streamer/statuses`, `v3/streamer/rtsp` (`{enabled, port: 8554}`), `v3/streamer/v4l2_devices`, `v3/streamer/{id}/config|status|screenshot`, `v2/streamer/status|rtsp|configs|video_devices`, `v1/ports/mavlink-router` (+`/status`), `v1/ports/gps`, `streamer/config|status`, `viewer/config|status`, `v1/video_recorder/*`. Запис (не викликались): `POST v3/streamer/config`, `PUT v3/streamer/{id}/config`, `PUT v2/streamer/rtsp`, `POST v1/ports/mavlink-router`, `POST settings/{password,auth,port,reboot,reset,update}`, `POST network/zerotier`, `POST v2/network/interface/{if}`. **Це замінює вигаданий стаб `:8080 /api/video` у C1a — parity-задача KSIM.**
- **Фактичний конфіг Streamer:** **два незалежні відеовходи** (`configs[0]`: USB-камера 640×480@30 MJPEG → H.264 2000 кбіт, I-кадр 10, RTSP `/video0`; `configs[1]`: UVC 1280×720@20 → H.264 2000 кбіт, I-кадр 20, RTSP `/video1`). Обидва входи — USB/UVC (`v4l2`), не AV/HDMI напряму; апаратний MJPEG-декодер і конвертація `hw`, `transcode_h264_enabled`. Профіль `h264_profile: 4`, `level: 11`, QP 26–51. RTP push увімкнений на `192.168.13.20:5600/5601` (Viewer, недоступний), `output_rtsp_push_to_local_enabled: true`. Статус: input 35 fps 2360 кбіт/с, output 315 кбіт/с (статична картинка), CPU 16 %. **MAVLink-роутер Streamer:** `enabled: false`, UART 115200, TCP-сервер `:5760` — окремий від FlyByIP-B механізм, потенційна альтернатива мосту до боксів. Peer Devices у UI: Viewer і FBI_A «Unavailable», FBI_B «Available».
